I will be creating the Battleship/Battleships game. The goal is to use TDD.

What still needs to be done:

The computer's logic should be improved so that hitting a ship will prompt it to attack nearby fields.

The UI should be improved and the alerts removed.
